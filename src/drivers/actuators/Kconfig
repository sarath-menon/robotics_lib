menuconfig RL_ACTUATORS
	bool "enable actuator drivers"
    default y
	help
	  Enables actuator drivers from robotics_lib

if RL_ACTUATORS
	source "$(LIB_PATH)/src/drivers/actuators/motors/Kconfig"
endif 